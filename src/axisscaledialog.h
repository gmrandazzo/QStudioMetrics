#ifndef AXISSCALEDIALOG_H
#define AXISSCALEDIALOG_H

#include <QDialog>

class AxisScaleDialog : public QDialog
{
    Q_OBJECT
public:
  AxisScaleDialog(QDialog *parent = 0);
  ~AxisScaleDialog();

signals:

public slots:
};

#endif // AXISSCALEDIALOG_H
